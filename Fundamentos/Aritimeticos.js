const [a, b, c, d] = [5, 7, 9, 3]

const soma = a + b + c + d
const subtracao = c - a
const multiplicacao = a * d
const divisao = c / d
const modulo = d % a

console.log( soma, subtracao, multiplicacao, divisao/*.toFixed*/, modulo )